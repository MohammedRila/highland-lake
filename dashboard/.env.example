VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
# Optional admin credentials should not be committed here.
# Keep real secrets in local `.env` or your deployment provider's environment variables.
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=change_me
